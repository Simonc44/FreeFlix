// This file is now primarily for exporting the initialization logic.
// The actual implementation is in init.ts to avoid circular dependencies.
export { initializeFirebase, areFirebaseCredentialsSet } from './init';
